server:
  port: 8000
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
  mapper-locations: classpath:eqlee/ctm/resource/*/dao/mapper/*.xml
spring:
  profiles:
    active: dev
  application:
    name: service-resource
  jmx:
    enabled: false
app:
  workerId: 9
  dataCenterId: 1
